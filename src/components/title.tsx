const ComponentTitle = ({ value }: { value: string }) => {
    return (
        <h1 className="text-[#3399FF] font-chubbo-bold md:text-5xl text-4xl text-pretty">{value}</h1>
    )
}

export default ComponentTitle